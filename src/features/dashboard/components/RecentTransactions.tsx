import { Badge } from "@/components/ui/badge";
import { DataTableContainer } from "@/components/common/DataTableContainer";
import { recentTransactions } from "../dashboard.data";
export function RecentTransactions() { return <DataTableContainer title="Recent transactions" description="Latest activity across SimKash services"><table className="w-full min-w-[720px] text-sm"><thead className="bg-muted/50 text-left text-muted-foreground"><tr>{["Reference", "Customer", "Type", "Amount", "Status", "Date"].map((heading) => <th key={heading} className="px-5 py-3 font-medium">{heading}</th>)}</tr></thead><tbody>{recentTransactions.map((transaction) => <tr key={transaction.id} className="border-t"><td className="px-5 py-4 font-medium">{transaction.id}</td><td className="px-5 py-4">{transaction.customer}</td><td className="px-5 py-4 text-muted-foreground">{transaction.type}</td><td className="px-5 py-4 font-medium">{transaction.amount}</td><td className="px-5 py-4"><Badge variant={transaction.status === "Successful" ? "secondary" : transaction.status === "Failed" ? "destructive" : "outline"}>{transaction.status}</Badge></td><td className="px-5 py-4 text-muted-foreground">{transaction.date}</td></tr>)}</tbody></table></DataTableContainer>; }
